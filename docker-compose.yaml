services:
  # --------------------------------------------------------
  # Traefik Reverse Proxy
  # --------------------------------------------------------
  traefik:
    image: traefik:v3.7
    command:
      - "--api.insecure=true"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      - "--entrypoints.web.http.redirections.entryPoint.to=websecure"
      - "--entrypoints.web.http.redirections.entryPoint.scheme=https"
      - "--providers.file.filename=/etc/traefik/dynamic.yaml"
    ports:
      - "80:80"
      - "443:443"
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./infra/.certs:/certs:ro
      - ./infra/traefik-dynamic.yaml:/etc/traefik/dynamic.yaml:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"

  # --------------------------------------------------------
  # Identity
  # --------------------------------------------------------
  dex:
    image: ghcr.io/dexidp/dex:v2.45.1
    volumes:
      - ./infra/dex-config.yaml:/etc/dex/config.docker.yaml
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.dex-billed.rule=Host(`dex.billedapparat.test`)"
      - "traefik.http.routers.dex-billed.entrypoints=websecure"
      - "traefik.http.routers.dex-billed.tls=true"
      - "traefik.http.services.dex-billed.loadbalancer.server.port=5556"

  # --------------------------------------------------------
  # Observability
  # --------------------------------------------------------
  openobserve:
    image: public.ecr.aws/zinclabs/openobserve:latest
    environment:
      - ZO_ROOT_USER_EMAIL=admin@example.com
      - ZO_ROOT_USER_PASSWORD=password123
    volumes:
      - o2_data:/data
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.observe-billed.rule=Host(`observe.billedapparat.test`)"
      - "traefik.http.routers.observe-billed.entrypoints=websecure"
      - "traefik.http.routers.observe-billed.tls=true"
      - "traefik.http.services.observe-billed.loadbalancer.server.port=5080"

  otel-collector:
    image: otel/opentelemetry-collector-contrib:latest
    volumes:
      - ./infra/otel-config.yaml:/etc/otel/config.yaml
      - ./backend/logs:/etc/app/logs:ro
    command: ["--config=/etc/otel/config.yaml"]
    ports:
      - "3117:4317" # OTLP gRPC (Needed for Go backend on host)
      - "3118:4318" # OTLP HTTP (Needed for Vite frontend on host)
    depends_on:
      - openobserve

  # --------------------------------------------------------
  # Data Stores
  # --------------------------------------------------------
  redis:
    image: redis:8-alpine
    container_name: billedapparat_redis
    ports:
      - "3151:6379" # Needed for Go backend on host
    volumes:
      - redis_data:/data
    command: redis-server --save 60 1 --loglevel warning

  redisinsight:
    image: redis/redisinsight:latest
    container_name: billedapparat_redisinsight
    environment:
      - RI_APP_PORT=5540
      - RI_APP_HOST=0.0.0.0
      - RI_ACCEPT_TERMS_AND_CONDITIONS=true
      - RI_REDIS_ALIAS0=Billedapparat-Redis
      - RI_REDIS_HOST0=billedapparat_redis
      - RI_REDIS_PORT0=6379
    volumes:
      - redisinsight_data:/data
    depends_on:
      - redis
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.redis-billed.rule=Host(`redis.billedapparat.test`)"
      - "traefik.http.routers.redis-billed.entrypoints=websecure"
      - "traefik.http.routers.redis-billed.tls=true"
      - "traefik.http.services.redis-billed.loadbalancer.server.port=5540"

  funkapparat-seed:
    image: ghcr.io/potibm/funkapparat:latest
    container_name: billedapparat_funkapparat_seed
    pull_policy: always
    command: database reset --force --seed
    volumes:
      - ./infra/funkapparat:/app/config
      - funkapparat_data:/app/data

  funkapparat:
    image: ghcr.io/potibm/funkapparat:latest
    container_name: billedapparat_funkapparat
    pull_policy: always
    volumes:
      - ./infra/funkapparat:/app/config
      - funkapparat_data:/app/data
    command: serve --otel-endpoint=otel-collector:4317
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.funkapparat-billed.rule=Host(`funkapparat.billedapparat.test`)"
      - "traefik.http.routers.funkapparat-billed.entrypoints=websecure"
      - "traefik.http.routers.funkapparat-billed.tls=true"
      - "traefik.http.services.funkapparat-billed.loadbalancer.server.port=8080"
    depends_on:
      funkapparat-seed:
        condition: service_completed_successfully
      redis:
        condition: service_started
      otel-collector:
        condition: service_started

  tidsapparat-seed:
    image: ghcr.io/potibm/tidsapparat:latest
    container_name: billedapparat_tidsapparat_seed
    pull_policy: always
    command: database reset --force --seed
    volumes:
      - ./infra/tidsapparat:/app/config
      - tidsapparat_data:/app/data

  tidsapparat:
    image: ghcr.io/potibm/tidsapparat:latest
    container_name: billedapparat_tidsapparat
    pull_policy: always
    volumes:
      - ./infra/tidsapparat:/app/config
      - tidsapparat_data:/app/data
    command: serve --otel-endpoint=otel-collector:4317
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.tidsapparat-billed.rule=Host(`tidsapparat.billedapparat.test`)"
      - "traefik.http.routers.tidsapparat-billed.entrypoints=websecure"
      - "traefik.http.routers.tidsapparat-billed.tls=true"
      - "traefik.http.services.tidsapparat-billed.loadbalancer.server.port=8080"
    depends_on:
      tidsapparat-seed:
        condition: service_completed_successfully
      redis:
        condition: service_started
      otel-collector:
        condition: service_started

volumes:
  o2_data:
  redis_data:
  redisinsight_data:
  funkapparat_data:
  tidsapparat_data:
