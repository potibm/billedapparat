import { BaseSocialShow } from "@admin/components/show/BaseSocialShow";
import { ShowProps } from "react-admin";

export const SocialTextShow = (props: ShowProps) => (
  <BaseSocialShow {...props} title="View Social Text" />
);
