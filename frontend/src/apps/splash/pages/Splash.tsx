import React from "react";
import BaseCard from "../components/BaseCard";

export const Splash: React.FC = () => {
  return (
    <BaseCard title="Welcome">
      <p>We will offer some options here in the future.</p>
    </BaseCard>
  );
};

export default Splash;
